; crypto.asm — the actual primitive (cipher / S-box / PRNG) that
; license.asm builds on. Keep it separate from license.asm so the
; "this is just a building block" and "this is the vulnerable use of
; the building block" are in different files, easy to point at later.
